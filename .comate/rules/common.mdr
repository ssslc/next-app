---
description: 通用开发规则指南
globs: 
alwaysApply: true
---

# 包管理器规范

- 本项目统一使用 `pnpm` 作为包管理器，禁止使用 `npm` 或 `yarn`。
- 安装依赖请使用 `pnpm add <package>` 或 `pnpm add -D <package>`。
- 运行脚本请使用 `pnpm run <script>` 或直接 `pnpm <script>`。
